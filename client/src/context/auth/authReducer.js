import {
    SUCCESSFUL_REGISTRATION,
    ERROR_REGISTRATION,
    GET_USER,
    SUCCESSFUL_LOGIN,
    ERROR_LOGIN,
    LOG_OUT
} from '../../types/index'

const AuthReducer= (state, action)=>{
    switch(action.type){

        default:
            return state;
    }
}

export default AuthReducer;